const Instructions = () => {
  return (
    <div className="flex justify-center bg-gradient-to-r from-indigo-700 to-blue-500 bg-[length:200%] px-3 py-1 text-center text-white">
      <div>
        Every user has predictable credentials like email: user1@gmail.com,
        password: asdasdasd <br /> Log into multiple users on multiple windows
        and chat.
      </div>
    </div>
  );
};

export default Instructions;
